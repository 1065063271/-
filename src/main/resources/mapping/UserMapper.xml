<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.bank.dao.UserMapper" >
  <resultMap id="BaseResultMap" type="com.bank.pojo.User" >
    <!--
      WARNING - @mbggenerated
      This element is automatically generated by MyBatis Generator, do not modify.
    -->
    <id column="id" property="id" jdbcType="INTEGER" />
    <result column="username" property="username" jdbcType="VARCHAR" />
    <result column="password" property="password" jdbcType="VARCHAR" />
    <result column="email" property="email" jdbcType="VARCHAR" />
    <result column="phone" property="phone" jdbcType="INTEGER" />
    <result column="birthday" property="birthday" jdbcType="DATE" />
    <result column="induc" property="induc" jdbcType="VARCHAR" />
  </resultMap>
  <select id="userLogin" parameterType="com.bank.pojo.User" resultType="com.bank.pojo.User">
    select * from usertable where username=#{username} and  password=#{password};
  </select>
  <select id="showUsers" resultType="com.bank.pojo.User">
    select * from usertable;
  </select>
  <insert id="dataSave" parameterType="com.bank.pojo.User">
    insert into usertable (username,password,email,phone,birthday,induc)
    values (#{username},#{password},#{email},#{phone},#{birthday},#{induc})
  </insert>
</mapper>